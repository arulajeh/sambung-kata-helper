# Database Configuration
# Format: postgres://user:password@host:port/database
# For external DB via proxy-network, use the service name or container name as host
DATABASE_URL=postgres://postgres:secret@postgres:5432/dictionary

# Node Environment
NODE_ENV=production
