version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "3000"
    env_file:
      - .env
    networks:
      - proxy-network
    restart: unless-stopped

networks:
  proxy-network:
    external: true
